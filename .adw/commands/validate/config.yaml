# Phase Configuration: validate
# Generated by ADW Init Wizard
#
# Uncomment settings to customize this phase.

enabled: true
# timeout_seconds: 600  # Phase-specific timeout

# input_files:  # Variable → file mappings for templates
#   prd: docs/prd.md
#   architecture: docs/architecture.md

# llm:
#   model: null  # Options: opus, sonnet, haiku
#   # Defaults: plan/validate→opus, build/ship→sonnet, document→haiku
