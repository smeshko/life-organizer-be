# Phase Configuration: validate
# Generated by ADW Init Wizard
#
# Uncomment settings to customize this phase.

enabled: true
# timeout_seconds: 600  # Phase-specific timeout

# pre_hook: null  # Shell command before phase
# post_hook: null  # Shell command after phase

# input_files:  # Variable → file mappings for templates
#   prd: docs/prd.md
#   architecture: docs/architecture.md

# === Validate Phase Settings ===
# enable_evidence: true  # Run evidence validator
# enable_review: true  # Run code review validator
# enable_tests: true  # Run test validator
# test_command: null  # Custom test command (auto-detect)
# test_timeout_seconds: 300  # Timeout for test execution
# review_prompt: null  # Path to custom review prompt
# review_focus: ["security", "error_handling", "edge_cases"]  # Code review focus areas
# max_iterations: 5  # Maximum validation loop iterations
# max_fix_attempts_per_issue: 2  # Max attempts per issue
# stall_threshold: 2  # Iterations before stall
# triage_mode: auto  # auto/manual/hybrid
# auto_dismiss_info: true  # Auto-dismiss info-level issues
